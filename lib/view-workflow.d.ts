/** Research presentation mapped to the native plugin's workflow v2 input. */
import type { JsonValue } from '@deepseek-ai/dsh-util-values';
import type { ResearchViewSnapshot } from './view-types.ts';
/** Fixed card geometry leaves separate slots for every experiment on the active page. */
export declare function researchWorkflow(snapshot: ResearchViewSnapshot, locale: 'en' | 'zh-CN'): Record<string, JsonValue>;
//# sourceMappingURL=view-workflow.d.ts.map